let productService;_cdd‍.x([["default",()=>_cdd‍.o]]);_cdd‍.w("../Services/productService",[["default",["productService"],function(v){productService=v}]]);

const getId=(req,res)=>{
    const {id}=req.params
    if (id==''){
        const productos= productService.getAll()
        res.send(productos)}
    else{
        const producto= productService.getId(id)
        res.send(producto)}
}

const getAll=(req,res)=>{
    const productos= productService.getAll()
    res.send(productos)
}

const post=(req,res)=>{
    const producto= req.body
    const productos= productService.post(producto)
    res.send(productos)
}
const replaceId=(req,res)=>{
    const {id}=req.params
    const producto= req.body
    const product= productService.replaceId(producto,id)
    res.send(product)
}

_cdd‍.d({
    getId:getId,
    getAll:getAll,
    post:post,
    replaceId:replaceId
});