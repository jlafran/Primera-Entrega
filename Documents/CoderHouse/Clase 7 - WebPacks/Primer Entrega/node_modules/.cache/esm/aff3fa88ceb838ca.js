let productRepositories;_ceb‍.x([["default",()=>_ceb‍.o]]);_ceb‍.w("../Repositories/productRepositories",[["default",["productRepositories"],function(v){productRepositories=v}]]);
const productos= new productRepositories()
const getAll=()=>{
    return productos.getProducts()
}
const getId=(id)=>{
    const products=productos.getProducts()
    return products.find((producto)=>producto.id==id)
}
const post=(product)=>{
    const products=productos.getProducts()
    let id=0
    products.map((producto) => {
        if (id < producto.id) {
            id = producto.id
        }
    })
    products.push({
        id: id=id+1,
        producto: product
    })
    return productos.writeProduct(products)

}
const replaceId=(produc,id)=>{
    const products=productos.getProducts()
	products.filter((producto)=>producto.id==id)
        .forEach((producto) => {
            producto.producto.title=produc.title
            producto.producto.price=produc.price
            producto.producto.url=produc.url
		})
    const carga=productos.writeProduct(products)
    return products.find((producto)=>producto.id==id)
}

_ceb‍.d({
    getAll:getAll,
    getId:getId,
    post:post,
    replaceId:replaceId
});